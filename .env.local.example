# Copy to .env.local and fill in real values. Do not commit .env.local.

# Supabase (server-only; never expose SUPABASE_SERVICE_ROLE_KEY to the client)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Example keys (no secrets – replace with your own in .env.local)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
